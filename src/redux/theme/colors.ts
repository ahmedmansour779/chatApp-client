import { themeType } from "../../types/colorsType";

export const colors: themeType = {
    dark: {
        primary: "test",
        seconder: "test",
        third: "test"
    },
    light: {
        primary: "test",
        seconder: "test",
        third: "test"
    }
}